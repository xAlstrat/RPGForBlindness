using UnityEngine;
using System.Collections;

public class KeyboardBattleController : PlayerBattleController {

    protected override void getMovement()
    {
        /*
        if (Input.GetKey(KeyCode.LeftArrow) || Input.GetKeyDown(KeyCode.LeftArrow))
        {
            cube.turnLeft();
        }
        else if (Input.GetKey(KeyCode.RightArrow) || Input.GetKeyDown(KeyCode.RightArrow))
        {
            cube.turnRight();
        }
        else if (Input.GetKey(KeyCode.UpArrow) || Input.GetKeyDown(KeyCode.UpArrow))
        {
            cube.turnAhead();
        }
        else if (Input.GetKey(KeyCode.DownArrow) || Input.GetKeyDown(KeyCode.DownArrow))
        {
            cube.turnDown();
        }
        else if (Input.GetKeyUp(KeyCode.J) || Input.GetKeyDown(KeyCode.J))
        {
            cube.attack();
        }
        else if (Input.GetKeyUp(KeyCode.K) || Input.GetKeyDown(KeyCode.K))
        {
            cube.hold();
        }
        */
    }
}
